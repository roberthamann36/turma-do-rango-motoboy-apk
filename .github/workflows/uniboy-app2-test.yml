name: Build UNIBOY APP2 v33
on:
  push:
    branches:
      - uniboy-app2-test
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Aplicar logo oficial UNIBOY
        run: |
          curl -fL --retry 3 "https://turmadorango.com.br/uploads/apps/uniboy/logo.png" -o app/src/main/res/drawable-nodpi/app_icon.png
          test -s app/src/main/res/drawable-nodpi/app_icon.png

      - name: Gerar versão 33
        run: |
          gradle clean assembleRelease --stacktrace -PversionCodeOverride=33 -PversionNameOverride=1.2.3
          mkdir -p build-output
          cp app/build/outputs/apk/release/app-release-unsigned.apk build-output/UNIBOY-Entregas-v33-unsigned.apk

      - name: Publicar APK sem assinatura
        uses: actions/upload-artifact@v4
        with:
          name: UNIBOY-Entregas-v33-unsigned
          path: build-output/UNIBOY-Entregas-v33-unsigned.apk
          if-no-files-found: error
